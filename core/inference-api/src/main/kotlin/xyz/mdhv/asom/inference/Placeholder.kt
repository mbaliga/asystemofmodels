package xyz.mdhv.asom.inference

// P0 placeholder — engine interface + NoopEngine stub land with the server work.
// v1 NEVER ships a real local engine; local-only returns 501 LOCAL_ENGINE_ABSENT (brief §2).
internal object InferenceApiPlaceholder
