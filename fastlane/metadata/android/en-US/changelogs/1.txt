First release. OpenAI-compatible local API, bring-your-own-key cloud routing,
binder-verified app pairing, and an egress ledger built from the same record the
API reports, so the dashboard and the response can never disagree.
